# Feature Flags -- toggle components on/off for experiments
# Reload at runtime via TUI without restart

# Core agents
perception_enabled: true
emotion_enabled: true
memory_enabled: true
planning_enabled: true

# Core mechanics
hysteresis_enabled: true
runtime_effects_enabled: true
feedback_loops_enabled: true
homeostatic_hysteresis_enabled: true

# Inner monologue & autonomy
self_reflection_enabled: true
autonomous_thoughts_enabled: true
internal_stimulus_enabled: true

# Experiment: emotion -> runtime mapping
emotion_affects_temperature: true
stress_narrows_context: true
pain_reduces_bandwidth: true

# Stage 3 -- governance & circuit breaker
governance_enabled: true
circuit_breaker_enabled: true

# Stage 11 -- constitutional auditor
constitution_enabled: true

# Stage 12 -- ML regulators (off by default; enable for experiments)
ml_regulators_enabled: false

# Stage 29 -- persistent intentions / goal stack
goal_stack_enabled: true
goal_pursuit_enabled: true

# Stage 32 -- lightweight self-learning context
self_learning_enabled: true

# Stage 15 -- LLM-driven REM consolidation (off by default; opt-in)
llm_rem_enabled: false

# Stage 4 -- persistence
persistence_enabled: true

# Stage 5 -- vector memory + provenance
memory_store_enabled: true
provenance_tracking_enabled: true

# Stage 6 -- cost tracking + model fallback
cost_tracking_enabled: true
model_fallback_enabled: true

# Stage 7 -- observability (off by default)
observability_enabled: false
rerun_enabled: false
otel_enabled: false

# Stage 8 -- NATS bus (off by default)
nats_bus_enabled: false

# Stage 9 -- sleep mode (off by default)
sleep_mode_enabled: false
memory_consolidation_enabled: false

# Stage 10 -- tools (off by default)
tools_enabled: false
